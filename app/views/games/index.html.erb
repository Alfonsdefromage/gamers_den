<div class="container">
  <div class="index-games">
    <% @games.each do |game| %>
      <%= link_to game_path(game.id) do  %>
        <%= image_tag game.cover_url, alt: "#{game.title} cover", class: "cover-img" %>
      <% end %>
    <% end %>
  </div>
</div>
